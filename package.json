{
  "name": "sequelize-auto-incident-demo",
  "version": "1.0.0",
  "description": "Reproduce sequelize-auto regeneration issue when FK is removed",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "init-db": "node scripts/init-db.js",
    "drop-fk": "node scripts/drop-fk.js",
    "generate-models": "sequelize-auto -o './models' -d app_db -h localhost -u admin -p 5432 -x admin123 -e postgres"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.18.0",
    "sequelize": "^6.37.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "sequelize-auto": "^0.8.8"
  }
}
